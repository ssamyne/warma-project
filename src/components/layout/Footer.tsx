import React from 'react';

import Contact from '../home/Contact';

const Footer = React.memo(() => {
  const footerElement = [<Contact />];
  return <React.Fragment></React.Fragment>;
});

export default Footer;
